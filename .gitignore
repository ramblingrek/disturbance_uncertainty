# Large input data — not for version control
downloaded_images/

# Python bytecode
__pycache__/
*.py[cod]

# macOS metadata
.DS_Store
.AppleDouble

# Jupyter checkpoints
.ipynb_checkpoints/

# Claude Code — local per-user settings and memory (not shared)
.claude/
memory/

# Experiment output — ignore individual run CSVs, keep master logger
**/experiment_logs/run_ID_*.csv

# Experiment output folders (figures, per-stack PNGs, metrics CSVs)
**/experiment_outputs/

# Virtual environments (if one is added later)
.venv/
venv/
env/
